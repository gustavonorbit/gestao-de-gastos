import 'dart:async';

Future<void> webDownloadCsv(String filename, String content) async {
  // Not running on web - stub.
  throw UnsupportedError('webDownloadCsv is only available on web builds');
}
